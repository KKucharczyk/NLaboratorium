#ifndef TABLICA_H
#define TABLICA_H

/*!
 * \file
 * \brief Definicja klasy Tablica
 * 
 * Klasa Tablica to klasa umozliwiająca tworzenie obiektow
 * przechowywujących zmienna plikową oraz ilość elementów 
 * przechowywanych w dynamicznych tablicach. 
*/

#include <iostream>
#include <fstream>
#include <string>
#include <ctime>
#include <chrono>
using namespace std;



class Tablica{
public:  
  /*!
   * \brief Ilość elementów w tworzonych listach
   * 
   * Pole zawiera ilość elementów, które znajdują się w tworzonych
   * metodą Pobierz_dane() dynamicznych tablicach. Również to 
   * właśnie ta funkcja inicjuje to pole.
   */
  int ilosc;
  
  /*!
   * \brief Plik
   * 
   * Pole zawiera zmienną umozliwiającą operacje plikowe, m.in.:
   * otwieranie, zamykanie, pobieranie danych. 
   */
  fstream plik;
  
  // Metody
  /*!
   * Umożliwia otwarcie pliku o zadanej nazwie.
   */
  void Otworz_plik(string nazwa_pliku);
  
  /*!
   * Umożliwia zamknięcie wcześniej otwartego pliku.
   */
  void Zamknij_plik();
  
  /*! 
   * Umożliwia pobranie danych z wcześniej otwartego pliku i
   * umieszczenie ich w specjalnie wykreowanych tablicach 
   * o odpowiedniej wielkości.
   */
  int* Pobierz_dane();
  
  /*!
   * Umożliwia przemnożenie elementów wysyłanej tablicy przez dwa,
   */
  void Pomnoz(int* Tablica);
};




#endif