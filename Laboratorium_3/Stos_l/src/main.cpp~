#include "stos.h"
#include "czas.h"

int main()
{
   Stos A;
   cout << A.isEmpty() << endl;
   A.push(2);
   A.push(2);
   A.push(2);
   A.push(2);
   A.push(2);
   cout << A.isEmpty() << endl;
   A.pop();
   A.pop();
   cout << A.size() << endl;
   
   
}